// PDF.js worker shim
self.onmessage = function(event) {
  // Just acknowledge receipt of messages
  self.postMessage({
    callback: event.data.callback,
    type: "ready"
  });
};

console.log("PDF.js worker shim initialized"); 